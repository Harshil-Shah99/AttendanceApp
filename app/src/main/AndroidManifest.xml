<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.attendanceapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminActivity" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".AdminRoomActivity" />
        <activity android:name=".RoomCreationActivity" />
        <activity android:name=".AdminNotifyActivity" />
        <activity android:name=".MemberLoginActivity" />
        <activity android:name=".MemberRoomActivity" />
        <activity android:name=".AdminHome2Activity" />
        <activity android:name=".AdminStatsActivity" />
        <activity android:name=".CardViewActivity"></activity>
    </application>


</manifest>